services:
  cursor-api:
    build: .
    container_name: cursor-api
    restart: unless-stopped
    user: "0:0"
    ports:
      - "3003:3000"
    env_file:
      - .env
    volumes:
      - cursor-api-data:/app/data

volumes:
  cursor-api-data:

